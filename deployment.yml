apiVersion: apps/v1
kind: Deployment
metadata:
  name: nodeappDeploy
  labels:
    app: nodeapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nodeapp
  template:
    metadata:
      labels:
        app: nodeapp 
    spec:
      containers:
      - name: nodeserver
        image: kodiidok/nodeapp:latest
        ports:
        - containerPort: 3000